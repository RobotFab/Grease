# Ignore everything at the repo root
/*

# Keep only these at the root
!/.gitignore
!/extension/
!/readme.md

# Inside extension/, still ignore dependencies
extension/node_modules/

# Old tsc output — superseded by esbuild dist/ bundle
extension/out/
